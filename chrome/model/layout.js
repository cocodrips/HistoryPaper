// Generated by CoffeeScript 1.6.3
(function() {
  this.Layout = (function() {
    function Layout() {}

    Layout.prototype.createLayout = function() {
      var data, h, w;
      w = screen.availWidth;
      h = screen.availHeight;
      data = this.calcPosition(w, h, 5, 5);
      return this.arrangeArticles(data);
    };

    Layout.prototype.calcPosition = function(width, height, row, col) {
      var ah, aw, c, historyObjects, obj, r, _i, _j;
      historyObjects = [];
      aw = width / row;
      ah = height / col;
      for (c = _i = 0; 0 <= col ? _i < col : _i > col; c = 0 <= col ? ++_i : --_i) {
        for (r = _j = 0; 0 <= row ? _j < row : _j > row; r = 0 <= row ? ++_j : --_j) {
          obj = new HistoryObject();
          obj.left = r * aw;
          obj.top = c * ah;
          obj.width = aw;
          obj.height = ah;
          historyObjects[(c * row) + r] = obj;
        }
      }
      console.log(historyObjects);
      return {
        arrangeArticles: function(data) {
          return console.log("layout");
        }
      };
    };

    return Layout;

  })();

}).call(this);
